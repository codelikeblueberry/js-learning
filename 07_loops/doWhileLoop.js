/*
 It Ensures that the code block is executed at least once, and then checks the 
 condition for subsequent iterations.
*/

var count = 0;

do {
  console.log(count);
  count++;
} while (count < 5);

let  [key,value ]= [1,2]

console.log(key,value);
