//  You can use while loops when the number of iterations is not known 

/*
In JavaScript, a while loop is another type of control flow statement that allows you to
 repeatedly execute a block of code as long as a specified condition evaluates to true.
 */
const condition =""
 while (condition) {
    // code to be executed
  }

/*
The condition is evaluated before each iteration, and if it returns true,
 the loop continues executing the code block. If the condition becomes false, 
 the loop terminates, and the program continues with the next statement after the loop.
*/

var count = 0;

while (count < 5) {
  console.log(count);
  count++;
}


let  [key,value ]= [1,2]

console.log(key,value);